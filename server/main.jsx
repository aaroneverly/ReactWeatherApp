import { Meteor } from 'meteor/meteor';

//import Items from '../imports/api/Items';

import Weather from '../imports/api/weather'

Meteor.startup(() => {
  // code to run on server at startup
  // console.log(Weather.zipCode, Weather.countryCode)
});
